package net.thevpc.common.props;

public enum PropertyUpdate {
    /**
     * used when the listener needs to be called when is registered
     */
    INIT,
    ADD,
    REMOVE,
    UPDATE,
    REFRESH,
}
